<div style="text-align:center; padding-top:20px;">
  <img src="{{ banner_src }}" alt="IAO Architecture" style="width:100%; border-radius:8px;" />
  <h1 style="font-size:36px; margin-top:24px;">{{ cap_id }} — {{ cap_name }}</h1>
  <h2 style="font-size:24px;">Architecture Document (TOGAF BDAT)</h2>
  <p style="font-size:18px; color:#555;">{{ tower_name }} ({{ tower_short }}) Tower<br/>
  Capability {{ cap_id }} · {{ l1_process }}</p>
  <p style="font-size:14px; color:#888;">IAO Program · {{ release_name }}<br/>
  Generated: {{ gen_date }}<br/>
  Sajiv Francis</p>
  <p style="font-size:12px; color:#aaa;">IAO Architecture Pipeline — Intel Confidential</p>
</div>

<style>
@media print {
  @page { size: A4; margin: 10mm 15mm; }
  .mermaid { page-break-inside: avoid; overflow: visible; }
  pre, table { page-break-inside: avoid; }
  h2, h3, h4 { page-break-after: avoid; }
  p { orphans: 3; widows: 3; }
  table { table-layout: fixed; word-wrap: break-word; font-size: 8pt; }
  td, th { overflow: hidden; text-overflow: ellipsis; padding: 3px 4px; }
}
.mermaid { overflow: visible; }
.mermaid svg { max-width: 100%; height: auto !important; }
nav.toc { margin: 16px 0 24px 0; }
nav.toc ol, nav.toc ul { list-style: none; padding-left: 0; margin: 0; }
nav.toc > ol > li { margin-bottom: 6px; font-weight: 600; font-size: 14px; }
nav.toc > ol > li > ul { padding-left: 28px; margin-top: 4px; }
nav.toc > ol > li > ul > li { font-weight: 400; font-size: 13px; margin-bottom: 2px; }
nav.toc a { color: #0071c5; text-decoration: none; }
nav.toc a:hover { text-decoration: underline; }
</style>

<div style="page-break-before: always;"></div>

<a id="toc"></a>

## Table of Contents

<nav class="toc">
<ol>
  <li><a href="#1-executive-summary">1. Executive Summary</a></li>
  <li><a href="#2-business-context-objectives">2. Business Context &amp; Objectives</a>
    <ul>
      <li><a href="#21-classification">2.1 Classification</a></li>
      <li><a href="#22-business-drivers">2.2 Business Drivers</a></li>
      <li><a href="#23-success-criteria">2.3 Success Criteria</a></li>
      <li><a href="#24-companion-documents">2.4 Companion Documents</a></li>
    </ul>
  </li>
  <li><a href="#3-business-architecture-togaf-b">3. Business Architecture (TOGAF &ldquo;B&rdquo;)</a>
    <ul>
      <li><a href="#31-business-process-overview">3.1 Business Process Overview</a></li>
      <li><a href="#32-business-process-diagrams">3.2 Business Process Diagrams</a></li>
      <li><a href="#33-business-roles-responsibilities">3.3 Business Roles &amp; Responsibilities</a></li>
      <li><a href="#34-process-readiness">3.4 Process Readiness</a></li>
    </ul>
  </li>
  <li><a href="#4-data-architecture-togaf-d">4. Data Architecture (TOGAF &ldquo;D&rdquo;)</a>
    <ul>
      <li><a href="#41-data-entities-ownership">4.1 Data Entities &amp; Ownership</a></li>
      <li><a href="#42-data-flow-diagrams">4.2 Data Flow Diagrams</a></li>
      <li><a href="#43-data-lineage">4.3 Data Lineage</a></li>
      <li><a href="#44-ricefw-data-objects">4.4 RICEFW Data Objects</a></li>
      <li><a href="#45-data-governance-quality">4.5 Data Governance &amp; Quality</a></li>
    </ul>
  </li>
  <li><a href="#5-application-architecture-togaf-a">5. Application Architecture (TOGAF &ldquo;A&rdquo;)</a>
    <ul>
{% if current_flows.hops %}
      <li><a href="#51-{{ current_label | lower | replace(' ', '-') }}-current-state-application-landscape">5.1 Current-State Application Landscape</a></li>
{% endif %}
{% if future_flows.hops %}
      <li><a href="#52-{{ future_label | lower | replace(' ', '-') }}-future-state-application-landscape">5.2 Future-State Application Landscape</a></li>
{% endif %}
{% if current_flows.hops and future_flows.hops %}
      <li><a href="#53-change-impact-summary">5.3 Change Impact Summary</a></li>
{% endif %}
      <li><a href="#54-component-overview">5.4 Component Overview</a></li>
      <li><a href="#55-development-object-inventory">5.5 Development Object Inventory</a>
        <ul>
          <li><a href="#551-sap-development-objects">5.5.1 SAP Development Objects</a></li>
          <li><a href="#552-eca-development-objects">5.5.2 ECA Development Objects</a></li>
          <li><a href="#553-interface-objects">5.5.3 Interface Objects</a></li>
          <li><a href="#554-middleware-objects">5.5.4 Middleware Objects</a></li>
          <li><a href="#555-scheduling-batch-objects">5.5.5 Scheduling &amp; Batch Objects</a></li>
          <li><a href="#556-boundary-application-dependencies">5.5.6 Boundary Application Dependencies</a></li>
        </ul>
      </li>
      <li><a href="#56-integration-patterns">5.6 Integration Patterns</a></li>
{% if has_release_flows %}
      <li><a href="#57-release-specific-architecture-{{ release_id | lower }}">5.7 Release-Specific Architecture ({{ release_id }})</a></li>
{% endif %}
    </ul>
  </li>
  <li><a href="#6-technology-architecture-togaf-t">6. Technology Architecture (TOGAF &ldquo;T&rdquo;)</a>
    <ul>
      <li><a href="#61-platform-infrastructure">6.1 Platform &amp; Infrastructure</a></li>
      <li><a href="#62-sap-development-object-status">6.2 SAP Development Object Status</a></li>
      <li><a href="#63-nfrs-design-principles">6.3 NFRs &amp; Design Principles</a></li>
      <li><a href="#64-security-governance">6.4 Security &amp; Governance</a></li>
      <li><a href="#65-eca-development-object-status">6.5 ECA Development Object Status</a></li>
    </ul>
  </li>
  <li><a href="#7-project-context">7. Project Context</a>
    <ul>
      <li><a href="#71-project-roadmap-go-live-plan">7.1 Project Roadmap &amp; Go-Live Plan</a></li>
      <li><a href="#72-raid-log">7.2 RAID Log</a></li>
      <li><a href="#73-recommendations-next-steps">7.3 Recommendations &amp; Next Steps</a></li>
    </ul>
  </li>
</ol>
</nav>

<div style="page-break-before: always;"></div>

## 1. Executive Summary

This Architecture Document defines the **Business, Data, Application, and Technology** (BDAT) architecture for **{{ cap_id }} {{ cap_name }}** within the IAO program.{% if bpmn_processes %} It includes {{ bpmn_processes|length }} BPMN process diagram(s) in Section 3.{% endif %}

| Dimension | Value |
|-----------|-------|
| **Tower** | {{ tower_name }} ({{ tower_short }}) |
| **Process Group** | {{ l1_process }} |
| **Capability** | {{ cap_id }} - {{ cap_name }} |
| **Release** | {{ release_name }} |
| **Total Systems** | {{ total_systems }} |
| **System Status** | {{ deployed_count }} Deployed, {{ developing_count }} Developing, {{ eol_count }} EOL, {{ no_match_count }} Pending IAPM |
{% if ricefw and ricefw.ricefw %}| **RICEFW Objects** | {{ ricefw.ricefw_summary }} |
{% else %}| **RICEFW Objects** | Pending — Smartsheet Object Tracker API integration |
{% endif %}
{% if current_flows.hops and future_flows.hops %}
**Change Summary**: {{ diff.new_count }} new flow chains, {{ diff.removed_count }} removed, {{ diff.modified_count }} modified, {{ diff.unchanged_count }} unchanged between {{ current_label }} and {{ future_label }} states.
{% endif %}

> All system nodes in architecture diagrams are **IAPM-linked** — click any node to open its IAPM page. Diagrams require `securityLevel: 'loose'` for click events.

<div style="page-break-before: always;"></div>

## 2. Business Context & Objectives

### 2.1 Classification

| Level | Value |
|-------|-------|
| **L0 Tower** | {{ tower_name }} |
| **L1 Process** | {{ l1_process }} |
| **L2 Capability** | {{ cap_id }} - {{ cap_name }} |

### 2.2 Business Drivers

{% if ctx and ctx.has_drivers %}
| # | Driver | Description | Strategic Alignment | Priority |
|---|--------|-------------|---------------------|----------|
{% for d in ctx.business_drivers -%}
| {{ d.number }} | {{ d.name }} | {{ d.description }} | {{ d.strategic_alignment }} | {{ d.priority }} |
{% endfor %}
{% else %}
*Business drivers for {{ cap_id }} {{ cap_name }} will be populated from the Input Portal when tower architects complete the Business Drivers tab.*
{% endif %}

{% if ctx and ctx.has_drivers %}
<div style="page-break-before: always;"></div>
{% endif %}

### 2.3 Success Criteria

{% if ctx and ctx.has_criteria %}
| Metric | Target | Measure | Baseline | Owner |
|--------|--------|---------|----------|-------|
{% for c in ctx.success_criteria -%}
| {{ c.metric }} | {{ c.target }} | {{ c.measure }} | {{ c.baseline }} | {{ c.owner }} |
{% endfor %}
{% else %}
*Success criteria for {{ cap_id }} {{ cap_name }} will be populated from the Input Portal when tower architects complete the Success Criteria tab.*
{% endif %}

### 2.4 Companion Documents

| Document | Description |
|----------|-------------|
| **Business Architecture** | Included in this document (Section 3) — process flows from BPMN diagrams |
| **This Document** | Full BDAT Architecture — Business + Data + Application + Technology |

<div style="page-break-before: always;"></div>

## 3. Business Architecture (TOGAF "B")

### 3.1 Business Process Overview

{% if bpmn_processes %}
This capability includes **{{ bpmn_processes|length }} business process(es)** modeled in BPMN 2.0, covering the end-to-end workflow for {{ cap_id }} {{ cap_name }}.

{{ bpmn_inventory_table }}

<div style="page-break-before: always;"></div>

### 3.2 Business Process Diagrams

{% set ns = namespace(page_nodes=0) %}
{% for proc in bpmn_processes %}
{% set is_large = proc.node_count > 12 or proc.lanes|length > 1 %}
{% if not loop.first and (is_large or ns.page_nodes + proc.node_count > 20) %}
<div style="page-break-before: always;"></div>
{% set ns.page_nodes = 0 %}
{% endif %}

#### BUSINESS ARCHITECTURE — 3.2.{{ loop.index }} {{ proc.step_id }} — {{ proc.step_name }}

{% if proc.lanes %}**Swim Lanes**: {{ proc.lanes | join(' · ') }}{% endif %}
 | **Tasks**: {{ proc.task_count }} | **Gateways**: {{ proc.gateway_count }}

> **Legend**: <span style="color:#000;background:#4CAF50;padding:2px 6px;border-radius:10px;font-weight:bold;font-size:9pt">● Start</span> · <span style="color:#fff;background:#C62828;padding:2px 6px;border-radius:10px;font-weight:bold;font-size:9pt">● End</span> · <span style="background:#E3F2FD;padding:2px 6px;border:1px solid #1565C0;font-size:9pt">User Task</span> · <span style="background:#FFF3E0;padding:2px 6px;border:1px solid #E65100;font-size:9pt">Service Task</span> · <span style="background:#FFF9C4;padding:2px 6px;border:1px solid #F57F17;font-size:9pt">◇ Gateway</span> · <span style="background:#F3E5F5;padding:2px 6px;border:1px solid #7B1FA2;font-size:9pt">Sub-Process</span>

```mermaid
{{ bpmn_mermaids[loop.index0] }}
```

{% set ns.page_nodes = ns.page_nodes + proc.node_count %}
{% endfor %}
{% else %}
*(No BPMN files found in input/bpmn/ for this capability.)*
{% endif %}

{% if bpmn_processes %}
<div style="page-break-before: always;"></div>
{% endif %}

### 3.3 Business Roles & Responsibilities

{% if bpmn_processes %}
{% set all_lanes = [] %}
{% for proc in bpmn_processes %}
{% for lane in proc.lanes %}
{% if lane not in all_lanes %}
{% if all_lanes.append(lane) %}{% endif %}
{% endif %}
{% endfor %}
{% endfor %}
{% if all_lanes %}
| Role / Lane | Processes Involved | Description |
|------------|-------------------|-------------|
{% for lane in all_lanes -%}
| {{ lane }} | {% for proc in bpmn_processes %}{% if lane in proc.lanes %}{{ proc.step_id }}{% if not loop.last %}, {% endif %}{% endif %}{% endfor %} | |
{% endfor %}
{% else %}
*(No lanes defined in BPMN models.)*
{% endif %}
{% else %}
*Business roles and responsibilities will be derived from BPMN swim lane assignments when process models are available.*
{% endif %}

{% if bpmn_processes %}
<div style="page-break-before: always;"></div>
{% endif %}

### 3.4 Process Readiness

{% if pr and pr.has_data %}
Process readiness assessment across **{{ pr.active_capabilities|length }} active L2 capabilities** with automated risk flags from Config Tracker, User Story Tracker, RICEFW Object Tracker{% if pr.has_transports %}, and SAP transport analysis{% endif %}.

| L2 | Description | L3 In-Scope | Config | User Stories | RICEFW | Status |
|----|-------------|:-----------:|:------:|:------------:|:------:|:------:|
{% for cap in pr.active_capabilities -%}
| {{ cap.l2_code }} | {{ cap.l2_desc|truncate(35) }} | {{ cap.l3_in_scope }}/{{ cap.l3_total }} | {% if cap.config_required %}{{ cap.config_pct|round(0)|int }}% ({{ cap.config_complete }}/{{ cap.config_required }}){% else %}—{% endif %} | {{ cap.us_fit }}F/{{ cap.us_gap }}G/{{ cap.us_total }} | {{ cap.ricefw_complete }}/{{ cap.ricefw_total }} | {{ cap.overall_icon }} |
{% endfor %}

{% if pr.has_transports %}
**Transport Status**:
{% for ts in pr.transports -%}
{{ ts.system }}: {{ ts.queued }} queued, {{ ts.released }} released, {{ ts.z_objects }} Z-objects |
{% endfor %}
{% endif %}

{% if pr.has_qa %}
**QA Promotion**:
{% for qv in pr.qa_verifications -%}
{{ qv.label }}: {{ qv.landed }}/{{ qv.dev_buffered }} promoted ({{ qv.promotion_pct|round(0)|int }}%) |
{% endfor %}
{% endif %}

{% if pr.total_flags > 0 %}
**Risk Flags**: {% if pr.high_flags %}🔴 {{ pr.high_flags }} HIGH{% endif %}{% if pr.medium_flags %} · 🟡 {{ pr.medium_flags }} MEDIUM{% endif %}{% if pr.low_flags %} · 🔵 {{ pr.low_flags }} LOW{% endif %}
{% endif %}

> 📄 **[Full Process Readiness Detail →]({{ cap_id }}-Process-Readiness.md)** — per-capability breakdown with config items, RICEFW objects, transport analysis, and QA verification.

{% else %}
*Process readiness data will be auto-populated when Config Tracker, User Story Tracker, and Object Tracker data are available for this tower.*
{% endif %}

<div style="page-break-before: always;"></div>

## 4. Data Architecture (TOGAF "D")

### 4.1 Data Flows — Source to Target

{% if data_flows %}
| # | Flow Chain | Hop | Source App | Target App | Data Description | Frequency |
|---|-----------|-----|-----------|-----------|-----------------|----------|
{% for df in data_flows -%}
| {{ loop.index }} | {{ df.flow_chain }} | {{ df.hop }} | {{ df.source_app }} | {{ df.target_app }} | {{ df.data_desc }} | {{ df.frequency }} |
{% endfor %}
{% else %}
*Data flows with DB platform details will be populated when tower architects complete the extended flow template columns (42-47) via the Input Portal.*
{% endif %}

{% if data_flows %}
<div style="page-break-before: always;"></div>
{% endif %}

### 4.2 Data Flow Diagrams

> **DATA ARCHITECTURE** — Database-to-database data flows. Applications (blue) sit above their hosting databases (green cylinders). Thick arrows show data movement between databases.

{% if current_data_arch_mermaid %}

#### 4.2.1 {{ current_label }} — Current-State Data Flows

```mermaid
{{ current_data_arch_mermaid }}
```

{% endif %}

{% if current_data_arch_mermaid and future_data_arch_mermaid %}
<div style="page-break-before: always;"></div>
{% endif %}

{% if future_data_arch_mermaid %}

#### 4.2.2 {{ future_label }} — Future-State Data Flows

```mermaid
{{ future_data_arch_mermaid }}
```

{% elif data_flow_mermaid %}

#### 4.2.2 {{ future_label }} — Future-State Data Flows

```mermaid
{{ data_flow_mermaid }}
```
{% endif %}

{% if current_data_arch_mermaid or future_data_arch_mermaid or data_flow_mermaid %}
<div style="page-break-before: always;"></div>
{% endif %}

### 4.3 Data Lineage

{% if data_lineage %}
| # | Source System | Source Schema/Object | Target System | Target Schema/Object | Transformation |
|---|-------------|---------------------|---------------|---------------------|---------------|
{% for dl in data_lineage -%}
| {{ loop.index }} | {{ dl.source_sys }} | {{ dl.source_obj }} | {{ dl.target_sys }} | {{ dl.target_obj }} | {{ dl.notes }} |
{% endfor %}
{% else %}
*Data lineage (source schema/object → target schema/object mappings) will be populated when tower architects provide validated schema details via the Input Portal.*
{% endif %}

### 4.4 RICEFW Data Objects

{% if ricefw and (ricefw.ricefw_by_type("R") or ricefw.ricefw_by_type("C")) %}
Data-centric RICEFW objects (Reports and Conversions) from the Object Tracker:

| Object ID | Type | Description | Status | Source → Target | Complexity |
|-----------|------|-------------|--------|----------------|----------|
{% for obj in ricefw.ricefw_by_type("R") + ricefw.ricefw_by_type("C") -%}
| {{ obj.object_id }} | {{ obj.type_label }} | {{ obj.description|tc }} | {{ obj.status|tc }} | {{ obj.source_system|tc }}{% if obj.target_system %} → {{ obj.target_system|tc }}{% endif %} | {{ obj.technical_complexity|tc }} |
{% endfor %}
{% else %}
*RICEFW data objects (Reports and Conversions) will be auto-populated from the Smartsheet Object Tracker when matched to this capability.*
{% endif %}

### 4.5 Data Governance & Quality

| Concern | Approach |
|---------|----------|
| Data Ownership | Per-entity owners listed in Section 3.1 |
| Data Classification | Financial data classified as Intel Confidential |
| Data Retention | Per Intel corporate retention policies |
| Data Quality | Validated at source; reconciliation at target |

<div style="page-break-before: always;"></div>

## 5. Application Architecture (TOGAF "A")

{% if current_flows.hops %}
### 5.1 {{ current_label }} — Current-State Application Landscape

#### Overview

The {{ current_label }} architecture represents the **current / legacy** landscape for {{ cap_id }}.{% if current_desc %} {{ current_desc }}{% endif %}
{% if current_flows.hops %}
This view is generated from `{{ current_csv_name }}` ({{ current_flows.total_hops }} flow hops across {{ current_flows.total_chains }} flow chains).
{% endif %}

{% if current_archimate_mermaid %}
#### APPLICATION ARCHITECTURE — Architecture Diagram

> **Click any system node** to open its IAPM application page.
> **Legend**: <span style="background:#C8E6C9;padding:2px 6px;border:1px solid #2E7D32;font-size:9pt">Deployed</span> · <span style="background:#E3F2FD;padding:2px 6px;border:1px solid #1565C0;font-size:9pt">Developing</span> · <span style="background:#FFCDD2;padding:2px 6px;border:1px solid #C62828;font-size:9pt">End-of-Life</span> · <span style="background:#ECEFF1;padding:2px 6px;border:1px solid #78909C;font-size:9pt;border-style:dashed">No IAPM Match</span>

```mermaid
{{ current_archimate_mermaid }}
```
{% elif current_mermaid %}
#### APPLICATION ARCHITECTURE — Architecture Diagram

> **Click any system node** to open its IAPM application page.
> **Legend**: <span style="background:#C8E6C9;padding:2px 6px;border:1px solid #2E7D32;font-size:9pt">Deployed</span> · <span style="background:#E3F2FD;padding:2px 6px;border:1px solid #1565C0;font-size:9pt">Developing</span> · <span style="background:#FFCDD2;padding:2px 6px;border:1px solid #C62828;font-size:9pt">End-of-Life</span> · <span style="background:#ECEFF1;padding:2px 6px;border:1px solid #78909C;font-size:9pt;border-style:dashed">No IAPM Match</span>

```mermaid
{{ current_mermaid }}
```
{% endif %}

{% if current_flows.chains %}
<div style="page-break-before: always;"></div>
{% endif %}

#### {{ current_label }} Flow Narrative

{% if current_flows.chains %}
| # | Flow Chain | Path | Interface | Freq |
|---|-----------|------|-----------|------|
{% for chain in current_flows.chains -%}
| {{ loop.index }} | {{ chain.name }} | {{ chain.path_str }} | {{ chain.interface_str }} | {{ chain.frequency }} |
{% endfor %}
{% else %}
*(No current-state flows defined.)*
{% endif %}

{% if current_flows.chains %}
<div style="page-break-before: always;"></div>
{% endif %}
{% endif %}{# end if current_flows.hops #}

{% if future_flows.hops %}
### 5.2 {{ future_label }} — Future-State Application Landscape

#### Overview

The {{ future_label }} architecture represents the **target** landscape for {{ cap_id }}.{% if future_desc %} {{ future_desc }}{% endif %}
{% if future_flows.hops %}
This view is generated from `{{ future_csv_name }}` ({{ future_flows.total_hops }} flow hops across {{ future_flows.total_chains }} flow chains).
{% endif %}

{% if future_archimate_mermaid %}
#### APPLICATION ARCHITECTURE — Architecture Diagram

> **Click any system node** to open its IAPM application page.
> **Legend**: <span style="background:#C8E6C9;padding:2px 6px;border:1px solid #2E7D32;font-size:9pt">Deployed</span> · <span style="background:#E3F2FD;padding:2px 6px;border:1px solid #1565C0;font-size:9pt">Developing</span> · <span style="background:#FFCDD2;padding:2px 6px;border:1px solid #C62828;font-size:9pt">End-of-Life</span> · <span style="background:#ECEFF1;padding:2px 6px;border:1px solid #78909C;font-size:9pt;border-style:dashed">No IAPM Match</span>

```mermaid
{{ future_archimate_mermaid }}
```
{% elif future_mermaid %}
#### APPLICATION ARCHITECTURE — Architecture Diagram

> **Click any system node** to open its IAPM application page.
> **Legend**: <span style="background:#C8E6C9;padding:2px 6px;border:1px solid #2E7D32;font-size:9pt">Deployed</span> · <span style="background:#E3F2FD;padding:2px 6px;border:1px solid #1565C0;font-size:9pt">Developing</span> · <span style="background:#FFCDD2;padding:2px 6px;border:1px solid #C62828;font-size:9pt">End-of-Life</span> · <span style="background:#ECEFF1;padding:2px 6px;border:1px solid #78909C;font-size:9pt;border-style:dashed">No IAPM Match</span>

```mermaid
{{ future_mermaid }}
```
{% endif %}

{% if future_flows.chains %}
<div style="page-break-before: always;"></div>
{% endif %}

#### {{ future_label }} Flow Narrative

{% if future_flows.chains %}
| # | Flow Chain | Path | Interface | Freq |
|---|-----------|------|-----------|------|
{% for chain in future_flows.chains -%}
| {{ loop.index }} | {{ chain.name }} | {{ chain.path_str }} | {{ chain.interface_str }} | {{ chain.frequency }} |
{% endfor %}
{% else %}
*(No future-state flows defined.)*
{% endif %}

{% if future_flows.chains %}
<div style="page-break-before: always;"></div>
{% endif %}
{% endif %}{# end if future_flows.hops #}

{% if current_flows.hops and future_flows.hops %}
### 5.3 Change Impact Summary

| Change Type | Flow Chain | Detail |
|-------------|-----------|--------|
{% for c in diff.changes -%}
| **{{ c.change_type }}** | {{ c.flow_chain }} | {{ c.detail }} |
{% endfor %}

**Totals**: {{ diff.summary_str }}
{% endif %}{# end if both current and future #}

### 5.4 Component Overview

#### System Inventory

| System | IAPM ID | Status |
|--------|---------|--------|
{% for sys in system_inventory -%}
| {{ sys.name }} | {{ sys.iapm_id }} | {{ sys.status }} |
{% endfor %}

### 5.5 Development Object Inventory

{% if ricefw and ricefw.ricefw %}
**Summary**: {{ ricefw.dev_object_summary }} | RICEFW: {{ ricefw.ricefw_summary }}

{% set sap_objs = ricefw.sap_dev_objects() %}
{% set eca_objs = ricefw.eca_dev_objects() %}
{% set intf_objs = ricefw.interfaces() %}
{% set mw_objs = ricefw.middleware_objects() %}
{% set sched_objs = ricefw.scheduling_objects() %}

{% if sap_objs %}
#### 5.5.1 SAP Development Objects

SAP platform objects (Reports, Interfaces, Conversions, Enhancements, Forms, Workflows) developed on S/4, MDG, or S/4 BOT:

| Object ID | Type | Description | Status | Dev System | Complexity |
|-----------|------|-------------|--------|-----------|----------|
{% for obj in sap_objs -%}
| {{ obj.object_id }} | {{ obj.type_label }} | {{ obj.description|tc }} | {{ obj.status|tc }} | {{ obj.development_system|tc }} | {{ obj.technical_complexity|tc }} |
{% endfor %}
{% endif %}

{% if eca_objs %}
#### 5.5.2 ECA Development Objects

**Enterprise Cloud Analytics (ECA)** platform objects (Databricks/Snowflake) — views, tables, and ETL jobs with Source or Target System = ECA:

| Object ID | Type | Description | Status | Source → Target | Complexity |
|-----------|------|-------------|--------|----------------|----------|
{% for obj in eca_objs -%}
| {{ obj.object_id }} | {{ obj.type_label }} | {{ obj.description|tc }} | {{ obj.status|tc }} | {{ obj.source_system|tc }}{% if obj.target_system %} → {{ obj.target_system|tc }}{% endif %} | {{ obj.technical_complexity|tc }} |
{% endfor %}
{% endif %}

{% if intf_objs %}
#### 5.5.3 Interface Objects

Holistic view of all interface objects by L2 capability — includes ECA → S/4, S/4 → ECA, boundary system, and inter-platform interfaces with middleware and integration approach:

| Object ID | Description | Source → Target | Middleware | Approach | Status |
|-----------|-------------|----------------|-----------|----------|--------|
{% for obj in intf_objs -%}
| {{ obj.object_id }} | {{ obj.description|tc }} | {{ obj.source_system|tc }}{% if obj.target_system %} → {{ obj.target_system|tc }}{% endif %} | {{ obj.middleware|tc }} | {{ obj.interface_approach|tc }} | {{ obj.status|tc }} |
{% endfor %}
{% endif %}

{% if mw_objs %}
#### 5.5.4 Middleware Objects

**MuleSoft** integration objects (Development System = MuleSoft):

| Object ID | Description | Status | Source → Target | Complexity |
|-----------|-------------|--------|----------------|----------|
{% for obj in mw_objs -%}
| {{ obj.object_id }} | {{ obj.description|tc }} | {{ obj.status|tc }} | {{ obj.source_system|tc }}{% if obj.target_system %} → {{ obj.target_system|tc }}{% endif %} | {{ obj.technical_complexity|tc }} |
{% endfor %}
{% endif %}

#### 5.5.5 Scheduling & Batch Objects

{% if sched_objs %}
| Object ID | Description | Status | Source → Target | Schedule | Complexity |
|-----------|-------------|--------|----------------|----------|----------|
{% for obj in sched_objs -%}
| {{ obj.object_id }} | {{ obj.description|tc }} | {{ obj.status|tc }} | {{ obj.source_system|tc }}{% if obj.target_system %} → {{ obj.target_system|tc }}{% endif %} | — | {{ obj.technical_complexity|tc }} |
{% endfor %}
{% else %}
*Scheduling and batch job objects (AutoSys, CWA) will be populated when job scheduler metadata is available. This section will map batch dependencies to RICEFW and ECA objects.*
{% endif %}

{% set ba_objects = [] %}
{% for obj in ricefw.ricefw if obj.boundary_app and obj.boundary_app != 'NA' %}
{% if ba_objects.append(obj) %}{% endif %}
{% endfor %}
{% if ba_objects %}
#### 5.5.6 Boundary Application Dependencies

The following development objects integrate with **boundary applications** (external systems outside the S/4 HANA core):

| Object ID | Description | Boundary Application | Source → Target |
|-----------|------------|---------------------|----------------|
{% for obj in ba_objects -%}
| {{ obj.object_id }} | {{ obj.description|tc }} | {{ obj.boundary_app|tc }} | {{ obj.source_system|tc }}{% if obj.target_system %} → {{ obj.target_system|tc }}{% endif %} |
{% endfor %}
{% endif %}
{% else %}
*Development object inventory will be auto-populated from the Smartsheet S/4 Object Tracker when matched to this capability.*
{% endif %}

{% if ricefw and ricefw.ricefw %}
<div style="page-break-before: always;"></div>
{% endif %}

### 5.6 Integration Patterns

{% if integration_patterns %}
| # | Pattern | Flow Chain | Middleware | Protocol | Auth |
|---|---------|-----------|-----------|----------|------|
{% for ip in integration_patterns -%}
| {{ loop.index }} | {{ ip.pattern }} | {{ ip.flow_chain }} | {{ ip.middleware }} | {{ ip.protocol }} | {{ ip.auth }} |
{% endfor %}
{% else %}
*Integration patterns will be populated when tower architects provide validated middleware and protocol details via the extended flow template.*
{% endif %}

{% if integration_patterns or has_release_flows %}
<div style="page-break-before: always;"></div>
{% endif %}

{% if has_release_flows %}
## 5.7 Release-Specific Architecture ({{ release_id }})

This section presents the **{{ release_id }}-scoped** current and future state flows, providing a focused view of the architecture changes planned for this release. The universal transformation analysis in §5.1–5.2 covers the full AS-IS → TO-BE picture; this section narrows to {{ release_id }}-specific scope.

{% if rel_current_flows.hops or rel_future_flows.hops %}
### 5.7.1 {{ release_id }} Current-State Flows

{% if rel_current_mermaid %}
{% if rel_current_archimate %}
#### {{ release_id }} Current-State Application Architecture

```mermaid
{{ rel_current_archimate }}
```

{% else %}
#### {{ release_id }} Current-State Flow

```mermaid
{{ rel_current_mermaid }}
```

{% endif %}

**{{ release_id }} Current-State Flow Summary**: {{ rel_current_flows.hops | length }} integration hops across {{ rel_current_flows.chains | length }} flow chains.
{% else %}
*(No {{ release_id }}-specific current-state flows defined.)*
{% endif %}

{% if rel_current_mermaid %}
<div style="page-break-before: always;"></div>
{% endif %}

### 5.7.2 {{ release_id }} Future-State Flows

{% if rel_future_mermaid %}
{% if rel_future_archimate %}
#### {{ release_id }} Future-State Application Architecture

```mermaid
{{ rel_future_archimate }}
```

{% else %}
#### {{ release_id }} Future-State Flow

```mermaid
{{ rel_future_mermaid }}
```

{% endif %}

**{{ release_id }} Future-State Flow Summary**: {{ rel_future_flows.hops | length }} integration hops across {{ rel_future_flows.chains | length }} flow chains.
{% else %}
*(No {{ release_id }}-specific future-state flows defined.)*
{% endif %}

{% if rel_diff %}
### 5.7.3 {{ release_id }} Change Impact

| Change Type | Flow Chain | Detail |
|-------------|-----------|--------|
{% for c in rel_diff.changes -%}
| **{{ c.change_type }}** | {{ c.flow_chain }} | {{ c.detail }} |
{% endfor %}

**{{ release_id }} Totals**: {{ rel_diff.summary_str }}
{% endif %}
{% endif %}
{% endif %}{# end if has_release_flows #}

{% if has_release_flows %}
<div style="page-break-before: always;"></div>
{% endif %}

## 6. Technology Architecture (TOGAF "T")

### 6.1 Platform & Infrastructure

> **TECHNOLOGY / PLATFORM ARCHITECTURE** — Platforms (green) host applications (blue). Thick arrows show platform-to-platform integration flows.

{% if current_platform_mermaid %}

#### 6.1.1 {{ current_label }} — Current-State Platform Architecture

```mermaid
{{ current_platform_mermaid }}
```

> **Legend**: <span style="background:#C8E6C9;padding:2px 8px;border:2px solid #388E3C;font-size:9pt">🖥️ Platform</span> · <span style="background:#B5DFFF;padding:2px 8px;border:2px solid #0077B6;font-size:9pt">📦 Application</span> · <span style="background:#FFB5B5;padding:2px 8px;border:2px solid #CC0000;font-size:9pt">⛔ End-of-Life</span> · <span style="background:#FFF9C4;padding:2px 8px;border:2px solid #F9A825;font-size:9pt">📋 Unassigned</span>

{% endif %}

{% if current_platform_mermaid and future_platform_mermaid %}
<div style="page-break-before: always;"></div>
{% endif %}

{% if future_platform_mermaid %}

#### 6.1.2 {{ future_label }} — Future-State Platform Architecture

```mermaid
{{ future_platform_mermaid }}
```

> **Legend**: <span style="background:#C8E6C9;padding:2px 8px;border:2px solid #388E3C;font-size:9pt">🖥️ Platform</span> · <span style="background:#B5DFFF;padding:2px 8px;border:2px solid #0077B6;font-size:9pt">📦 Application</span> · <span style="background:#FFB5B5;padding:2px 8px;border:2px solid #CC0000;font-size:9pt">⛔ End-of-Life</span> · <span style="background:#FFF9C4;padding:2px 8px;border:2px solid #F9A825;font-size:9pt">📋 Unassigned</span>

{% endif %}

#### Platform Inventory

{% if tech_platforms %}
| # | Platform | Type | Systems Using | Environment |
|---|----------|------|--------------|-------------|
{% for tp in tech_platforms -%}
| {{ loop.index }} | {{ tp.name }} | {{ tp.type }} | {{ tp.systems }} | {{ tp.environment }} |
{% endfor %}
{% else %}
*Platform inventory will be populated when tower architects provide validated technology platform details via the extended flow template.*
{% endif %}

{% if current_platform_mermaid or future_platform_mermaid or tech_platforms %}
<div style="page-break-before: always;"></div>
{% endif %}

### 6.2 SAP Development Object Status

{% if ctx and ctx.has_sap_status %}
{% set pivot = ctx.sap_pivot() %}
| Metric | DEV | QAS | PRD |
|--------|-----|-----|-----|
{% for obj_type, envs in pivot.items() -%}
| {{ obj_type }} | {{ envs.get('DEV', '—') }} | {{ envs.get('QAS', '—') }} | {{ envs.get('PRD', '—') }} |
{% endfor %}
{% else %}
*SAP development object status will be auto-populated when SAP OData/ADT API access is provisioned.*
{% endif %}

### 6.3 NFRs & Design Principles

{% if ctx and ctx.has_nfrs %}
| Category | Requirement | Target / SLA | Priority |
|----------|-------------|-------------|----------|
{% for n in ctx.nfrs -%}
| {{ n.category }} | {{ n.requirement }} | {{ n.target_sla }} | {{ n.priority }} |
{% endfor %}
{% else %}
*NFRs and design principles will be populated from the Input Portal when tower architects complete the NFRs tab.*
{% endif %}

### 6.4 Security & Governance

{% if ctx and ctx.has_security %}
| Concern | Approach | Standard / Policy | Owner |
|---------|----------|--------------------|-------|
{% for s in ctx.security_controls -%}
| {{ s.concern }} | {{ s.approach }} | {{ s.standard_policy }} | {{ s.owner }} |
{% endfor %}
{% else %}
*Security and governance controls will be populated from the Input Portal when tower architects complete the Security Controls tab.*
{% endif %}

### 6.5 ECA Development Object Status

{% if ricefw and ricefw.eca_dev_objects() %}
| Metric | Count |
|--------|-------|
| ECA Objects (this capability) | {{ ricefw.eca_dev_objects()|length }} |
| ECA Interfaces | {{ ricefw.eca_dev_objects()|selectattr('type_code', 'equalto', 'I')|list|length }} |
| Total ECA + SAP Interface Dependencies | {{ ricefw.interfaces()|length }} |
{% else %}
*ECA development object status will be auto-populated when Snowflake SELECT access is provisioned for the PDH-IF and PDH-IP curated layers. This section will provide a DEV/QAS/PRD maturity assessment equivalent to §6.2 for SAP objects.*
{% endif %}

{% if (ctx and (ctx.has_sap_status or ctx.has_nfrs or ctx.has_security)) or tech_platforms or current_platform_mermaid or future_platform_mermaid %}
<div style="page-break-before: always;"></div>
{% endif %}

## 7. Project Context

### 7.1 Project Roadmap & Go-Live Plan

{% if ricefw and ricefw.timeline %}
| ID | Description | FS | TDD | Build | FUT | Status |
|----|-------------|----|-----|-------|-----|--------|
{% for tl in ricefw.timeline[:20] -%}
| {{ tl.object_id }} | {{ tl.description|tc }} | {{ phase_cell(tl.fs_end, tl.fs_pct) }} | {{ phase_cell(tl.tdd_end, tl.tdd_pct) }} | {{ phase_cell(tl.build_end, tl.build_pct) }} | {{ phase_cell(tl.fut_end, tl.fut_pct) }} | {{ tl.overall_status|tc(20) }} |
{% endfor %}
{% if ricefw.timeline|length > 20 %}
*... and {{ ricefw.timeline|length - 20 }} more objects (see full Object Tracker)*
{% endif %}
{% else %}
*Project roadmap and RICEFW timelines will be auto-populated from the Smartsheet Object Tracker when matched to this capability.*
{% endif %}

{% if ricefw and ricefw.timeline %}
<div style="page-break-before: always;"></div>
{% endif %}

### 7.2 RAID Log

{% if ricefw and ricefw.raid %}
| # | Object ID | Category | RAID Description | Status | Assigned To |
|---|-----------|----------|-----------------|--------|-------------|
{% for r in ricefw.raid -%}
| {{ loop.index }} | {{ r.object_id }} | {{ r.category|tc }} | {{ r.raid_text|tc(50) }} | {{ r.status|tc }} | {{ r.assigned_to|tc }} |
{% endfor %}
{% else %}
*RAID items will be auto-populated from the Smartsheet RAID log when matched to this capability.*
{% endif %}

### 7.3 Recommendations & Next Steps

{% if ctx and ctx.has_recommendations %}
| # | Category | Recommendation | Priority | Owner | Target Date | Status |
|---|----------|---------------|----------|-------|-------------|--------|
{% for r in ctx.recommendations -%}
| {{ r.number }} | {{ r.category }} | {{ r.recommendation }} | {{ r.priority }} | {{ r.owner }} | {{ r.target_date }} | {{ r.status }} |
{% endfor %}
{% else %}
*Recommendations and next steps will be populated from the Input Portal when tower architects complete the Recommendations tab.*
{% endif %}

---
*{{ cap_id }} — Architecture Document (TOGAF BDAT) · {{ tower_name }} · Generated: {{ gen_date }}*
